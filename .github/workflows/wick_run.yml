name: Full_Market_Wick_Scan
on:
  schedule:
    # 💡 台灣時間 (UTC+8) 盤中監控排程：
    # 01:20 UTC = 09:20 TW
    # 每 20 分鐘執行一次，涵蓋 09:20 - 13:40
    - cron: "20,40 1-5 * * 1-5" 
    - cron: "0 2-5 * * 1-5"
  workflow_dispatch: # 允許手動點擊執行測試
jobs:
  run-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          pip install yfinance pandas requests FinMind tqdm
          
      - name: Run Efficient Scan
        # 執行剛才更新過的「精簡過濾版」程式
        run: python wick_scan.py
        
      - name: Sync Cache to GitHub
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main
          git add sent_wick_spikes.txt || echo "no cache"
          git commit -m "Update wick scan cache" || echo "no changes"
          git push origin main
