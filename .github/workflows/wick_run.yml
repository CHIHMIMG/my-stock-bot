name: Full_Market_Wick_Scan
on:
  schedule:
    # 💡 台灣時間 (UTC+8) 09:05 - 13:35 每 5 分鐘執行一次
    # UTC 01:05-05:35 對應台灣 09:05-13:35
    - cron: "5/5 1-5 * * 1-5"
  workflow_dispatch: # 允許手動點擊執行測試
jobs:
  run-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          # 這裡維持安裝必要的套件，確保執行環境穩定
          pip install yfinance pandas requests FinMind tqdm
          
      - name: Run Precision Scan
        # 執行我們剛才強化過、確保成交量與價格準確的程式
        run: python wick_scan.py
        
      - name: Sync Cache to GitHub
        # 這裡非常重要：確保已通知過的股票不會在 5 分鐘後又報一次洗版
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main
          git add sent_wick_spikes.txt || echo "no cache"
          git commit -m "Update scan cache" || echo "no changes"
          git push origin main
