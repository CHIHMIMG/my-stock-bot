name: Spike_Shadow_Monitor

on:
  schedule:
    - cron: "*/5 1-5 * * 1-5" # 台灣時間 09:05 - 13:35，每 5 分鐘掃描
  workflow_dispatch: 

jobs:
  spike-check:
    runs-on: ubuntu-latest
    permissions:
      contents: write 
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          # 💡 加入 tqdm 以防 FinMind 導入失敗
          pip install yfinance pandas requests FinMind tqdm

      - name: Run Spike Scan
        run: python spike_scan_all.py

      - name: Sync and Update Sent Cache
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # 💡 解決 Rejected 錯誤關鍵：先拉取變更
          git pull --rebase origin main
          git add sent_spikes.txt
          git commit -m "Update sent cache [skip ci]" || echo "no changes"
          git push origin main
